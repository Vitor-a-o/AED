#include <stdio.h>
#include <stdlib.h>

int* inorderTraversal(struct TreeNode* root, int* returnSize);
void pesquisa(struct );

struct TreeNode {
    int val;
    struct TreeNode *left;
    struct TreeNode *right;
};

int main(){
    struct TreeNode* root;

}

int* inorderTraversal(struct TreeNode* root, int* returnSize) {
    
}